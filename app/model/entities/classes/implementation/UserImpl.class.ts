'use strict';
import {AbstractUser} from "../abstract/AbstractUser.class";
/**
 * Created by Mor on 16/09/2016.
 */
export class UserImpl extends AbstractUser {
    constructor(obj?: UserImpl) {
        super(obj);
    }
}